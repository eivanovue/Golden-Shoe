export class ProductSize {
  id: number;
  size: number;
  stock: number;

  constructor(id: number, size: number, stock: number) {
    this.id = id;
    this.size = size;
    this.stock = stock;
  }
}
